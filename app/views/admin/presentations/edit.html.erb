<% content_for :title, "Edit Presentation" %>

<div class="max-w-5xl mx-auto">
  <h1 class="text-3xl font-bold mb-6">Edit Presentation</h1>

  <%= render "form", presentation: @presentation %>

  <%= link_to "Back", admin_presentation_path(@presentation), class: "mt-4 inline-block #{link_classes}" %>
</div>
